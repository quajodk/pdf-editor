# Copy to .env.local for local development.
# Vite only exposes variables that start with VITE_ to the client bundle.

# Backend base URL. The current app is fully client-side and does not call
# this yet, but the wiring is in place for future server integration.
VITE_API_URL=http://localhost:3001
